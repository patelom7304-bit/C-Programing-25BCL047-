#include <stdio.h>
 int main () {
 int a,b,product;
    printf ("Give your dear two numbers:\n");
    scanf ("%d %d",&a,&b );
    product = a*b;
    printf ("the product of %d and %d is = %d\n", a, b,product );
 return 0;
 }
