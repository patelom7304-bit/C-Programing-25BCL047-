#include <stdio.h>
 int main () {
 float a,b,quotient;
    printf ("Give your dear two numbers:\n");
    scanf ("%f %f",&a,&b );
    quotient = a/b;
    printf ("the quotient of %f and %f is = %f\n", a, b,quotient );
 return 0;
 }
